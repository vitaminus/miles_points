require "spec_helper"

describe MilesPoints do
  it "has a version number" do
    expect(MilesPoints::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
